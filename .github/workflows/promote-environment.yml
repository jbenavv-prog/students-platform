name: promote-environment-deprecated

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest

    steps:
      - name: Explain the active workflow
        run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/promote-qa.yml for the active two-environment flow (dev + qa)."
          exit 1
